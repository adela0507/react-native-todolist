import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
    root:{
        flexDirection:"row",
        flex:1,
        justifyContent:"space-evenly",
        alignItems:"center",
    },
});